export const plans = [
    {
        link: process.env.NODE_ENV === 'development' ? '' : '',
        priceId: process.env.NODE_ENV === 'development' ? '' : '',
        price: 7,
        duration: '/month'
    },
    {
        link: process.env.NODE_ENV === 'development' ? '' : '',
        priceId: process.env.NODE_ENV === 'development' ? '' : '',

        price: 70,
        duration: '/year'
    }
];
